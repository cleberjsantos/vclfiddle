language: bash
sudo: true
dist: xenial

services:
    - docker

addons:
  apt:
    packages:
    - pkg-config
    - cmake

before_install:
  - mkdir /vagrant

script:
  - ./bootstrap.sh

# vim:set et ts=2 sw=2:
